<%= form_for([micropost, micropost.comments.build]) do |f| %>
  <div class="form-group">
    <%= f.text_area :content %>
  </div>
  <div class="form-group">
     <%= f.submit "Add Comment" %>
  </div>
<% end %>