<li class="dropdown">
  <a id="notification-dropdown" href="#" class="dropdown-toggle" data-toggle="dropdown"> 
    <span class='red-number'> <%= current_user.notifications.unread_notifications.count %> </span> 
  </a>
  <ul class="dropdown-menu">
    <% if(current_user.notifications.any?) %>
      <% current_user.notifications.sort_notifications.each do |notification|%>
        <%= render "notifications/notification", notification: notification %>
      <% end %>
    <% else %>
      <li class="dropdown"> Notifications not avaliable! </li>
    <% end %>
  </ul>
</li>
