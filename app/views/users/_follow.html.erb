<%= form_for(current_user, url: follow_user_path, html: { method: :post }, remote: true) do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>