<%= form_for(current_user, url: unfollow_user_path, html: { method: :delete }, remote: true) do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>